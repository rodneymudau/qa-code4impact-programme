# Templates

Reusable templates referenced across facilitator and student guides — e.g. the blank Test Case template used from Week 2 onward.

Nothing here yet. When a template is used across more than one session, pull it out into its own file here (e.g. `test-case-template.docx`) so it's not duplicated inside multiple guides.
